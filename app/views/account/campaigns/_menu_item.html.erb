<% if can? :read, Campaign.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :campaigns]),
    label: t('campaigns.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('campaigns.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
