<% if can? :read, Subscriber.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :subscribers]),
    label: t('subscribers.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('subscribers.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
