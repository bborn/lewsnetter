en:
  segments: &segments
    label: &label Segments
    breadcrumbs:
      label: *label
    navigation:
      label: *label
      icon: fal fa-puzzle-piece
    buttons: &buttons
      new: Add New Segment
      create: Create Segment
      edit: Edit Segment
      update: Update Segment
      destroy: Remove Segment
      shorthand:
        edit: Edit
        destroy: Delete
      confirmations:
        # TODO customize for your use-case.
        destroy: Are you sure you want to remove %{segment_name}? This will also remove any child resources and can't be undone.
    segment:
      buttons: *buttons
    fields: &fields
      id:
        heading: Segment ID
      team_id:
        heading: Team ID
      name:
        _: &name Name
        label: *name
        heading: *name
        api_title: *name
        api_description: *name
      natural_language_source:
        _: &natural_language_source Natural Language Source
        label: *natural_language_source
        heading: *natural_language_source
        api_title: *natural_language_source
        api_description: *natural_language_source
      # 🚅 super scaffolding will insert new fields above this line.
      created_at:
        _: &created_at Added
        label: *created_at
        heading: *created_at
      updated_at:
        _: &updated_at Updated
        label: *updated_at
        heading: *updated_at
    api:
      collection_actions: "Collection Actions for Segments"
      index: "List Segments"
      create: "Add a New Segment"
      member_actions: "Actions for an Individual Segment"
      show: "Retrieve a Segment"
      update: "Update a Segment"
      destroy: "Delete a Segment"
      fields: *fields
    index:
      section: "%{teams_possessive} Segments"
      contexts:
        team:
          header: Segments
          description: Saved targets for campaigns. Filter your audience down for specific sends — or leave a campaign's Segment blank to send to everyone subscribed.
          description_empty: "Build your first segment. Saved targets for campaigns — send to just your power users, just paid customers, or just folks who haven't opened recent emails."
      fields: *fields
      buttons: *buttons
    show:
      section: "%{segment_name}"
      header: Segment
      description: Details for this segment.
      manage_description: Update the description or remove this segment.
      fields: *fields
      buttons: *buttons
    form: &form
      buttons: *buttons
      fields: *fields
    new:
      section: "Create a segment"
      header: Create a segment
      description: Describe the audience you want to reach. Lewsnetter will translate it into a saved query you can reuse across campaigns.
      form: *form
    edit:
      section: "%{segment_name}"
      header: Edit segment
      description: Update this segment's description or predicate.
      form: *form
    notifications:
      created: Segment was successfully created.
      updated: Segment was successfully updated.
      destroyed: Segment was successfully destroyed.
  account:
    segments: *segments
  activerecord:
    attributes:
      segment:
        name: *name
        natural_language_source: *natural_language_source
        # 🚅 super scaffolding will insert new activerecord attributes above this line.
        created_at: *created_at
        updated_at: *updated_at
